# Trading Bot Configuration
# Copy this file to .env and update with your values

# ⚠️  IMPORTANT: For live trading, only CCXT is supported for now!
# Set DATA_SOURCE=ccxt and USE_SANDBOX=false for real money trading

# Data Source Settings
DATA_SOURCE=ccxt                   # REQUIRED: ccxt (live trading only supports CCXT)
EXCHANGE_NAME=binance              # binance, kraken, coinbase, bybit, etc.
CSV_DATA_DIRECTORY=data/csv        # Directory for CSV files (backtest only)
SYMBOLS=BTC/USDT,ETH/USDT          # Comma-separated crypto pairs
TIMEFRAME=1h                       # 1m, 5m, 15m, 1h, 4h, 1d
USE_SANDBOX=true                   # true=paper trading, false=real money

# Strategy Settings
STRATEGY_NAME=sma                  # sma, mean_reversion, breakout, test (test=alternating buy/sell)
STRATEGY_SHORT_WINDOW=20           # Short moving average window
STRATEGY_LONG_WINDOW=50            # Long moving average window
STRATEGY_STOP_LOSS_PCT=2.0         # Strategy stop loss percentage (2%)
STRATEGY_TAKE_PROFIT_PCT=4.0       # Strategy take profit percentage (4%)

# Breakout Strategy (if using breakout)
STRATEGY_ENTRY_LOOKBACK=20         # Breakout entry lookback period
STRATEGY_EXIT_LOOKBACK=10          # Breakout exit lookback period
STRATEGY_ATR_PERIOD=14             # ATR period for volatility
STRATEGY_ATR_MULTIPLIER=2.0        # ATR multiplier for breakout

# Risk Management
INITIAL_CAPITAL=10000.0            # Starting capital
MAX_POSITION_SIZE=0.1              # Maximum position size (10% of capital)
STOP_LOSS_PCT=0.05                 # Global stop loss percentage (5%)

# Trading Settings
COMMISSION=0.001                   # Commission rate (0.1% spot taker, 0.04% futures taker)
SLIPPAGE=0.0005                   # Slippage factor (0.05%)
ALLOW_SHORT=false                  # true/false - Allow short selling
TRADING_TYPE=spot                  # spot or future

# API Keys (Required for live trading)
# Get these from your exchange's API section
# For testing: use testnet/sandbox API keys
# For live trading: use production API keys
EXCHANGE_API_KEY=your_api_key_here
EXCHANGE_API_SECRET=your_api_secret_here

# Monitoring
ALERT_EMAIL=your_email@example.com # Email for alerts
LOG_LEVEL=INFO                     # DEBUG, INFO, WARNING, ERROR

# Database Configuration
DB_HOST=postgres                   # PostgreSQL host (use 'postgres' for Docker)
DB_PORT=5432                       # PostgreSQL port
DB_NAME=trading_bot               # Database name
DB_USER=trading_user              # Database user
DB_PASSWORD=trading_password      # Database password - CHANGE THIS!

# Grafana Configuration
GRAFANA_PASSWORD=admin            # Grafana admin password - CHANGE THIS!

# Metrics Configuration
METRICS_PORT=8000                 # Port for Prometheus metrics endpoint
METRICS_HOST=0.0.0.0             # Host for metrics server
AUTO_START_METRICS_SERVER=true   # Auto-start metrics server with bot